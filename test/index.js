var mocha = require('mocha'),
    chai = require('chai').expect,
    sinon = require('sinon'),

    badger = require('../src/js/badger'),
    audio = require('../src/js/audio');

describe('badger module', function () {
  before(function () {
   sinon.spy(audio, )
  });

  it('.play()', function () {
    audio.play();
    expect()
  });
});